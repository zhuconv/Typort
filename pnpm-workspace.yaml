packages:
  - "apps/*"
  - "packages/*"

# Git-hosted deps need explicit opt-in to run `prepare` (which builds
# dist/lib on install). Required for open-typora@github:zhuconv/open-typora.
onlyBuiltDependencies:
  - open-typora
